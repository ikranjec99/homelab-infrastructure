services:
  bentopdf:
    image: bentopdf/bentopdf:latest
    container_name: bentopdf
    ports:
      - "${BENTOPDF_HTTP_PORT:-3000}:8080"
    restart: unless-stopped
    networks:
      - homelab

networks:
  homelab:
    driver: bridge
